services:
  goapp:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: goapp-service
    restart: unless-stopped
    volumes:
      - goapp-logs:/var/log
    
volumes:
  goapp-logs:
